import Button from "@mui/material/Button"

const HelpSupport = () => {
    
    return (
        <Button variant="contained" color="primary">Click me helpSupport</Button>
    )
}

export default HelpSupport;